/*#include<iostream>
using namespace std;
#define int double


int32_t main() {

	int x, y;
	cin >> x >> y;
	int s = 0, e = x;

	//Approach 1:

	int ans = y;
	int count = 0;

	while (s <= e) {

		int mid = (s + e) / 2;

		if ("Random") {
			s = mid;
			ans = mid;
		} else {
			e = mid;
			ans = mid;
		}

		count++;
		if (count > 100) {
			break;
		}
	}

	cout << ans << endl;

	int ans = y;
	int count = 0;

	while (s <= e) {

		int mid = (s + e) / 2;

		if ("Random") {
			s = mid + Error;
			ans = mid;
		} else {
			e = mid - Error;
			ans = mid;
		}

		count++;
		if (count > 100) {
			break;
		}
	}

	cout << ans << endl;


	int ans = y;
	int count = 0;

	while (e - s > Error) {

		int mid = (s + e) / 2;

		if ("Random") {
			s = mid;
			ans = mid;
		} else {
			e = mid;
			ans = mid;
		}
	}

	cout << ans << endl;


	int ans = y;
	int count = 0;

	while (e - s > Error) {

		int mid = (s + e) / 2;

		if ("Random") {
			s = mid + Error;
			ans = mid;
		} else {
			e = mid - Error;
			ans = mid;
		}
	}

	cout << ans << endl;

}*/